server {
    listen 80;
    server_name {{ host }};

    location / {
        proxy_set_header Host {{ host }};
        proxy_pass http://unix:/tmp/{{ host }}.socket;
    }

    location /static {
        alias {{ home }}/sites/{{ host }}/static;
    }
}

# vim: ft=ansible_template
